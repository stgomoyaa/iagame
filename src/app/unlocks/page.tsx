import { Desbloqueos } from '@/ui/Desbloqueos'

export const metadata = {
  title: 'Desbloqueos · Arcade FPS',
  description: 'Qué arma se abre en qué nivel de cuenta, y el catálogo por origen.',
}

// Ruta nueva de la fase 5 (desbloqueos por nivel del diseño de progresión).
export default function UnlocksPage() {
  return <Desbloqueos />
}
